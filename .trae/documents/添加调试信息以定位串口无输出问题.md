抱歉，我之前只是提供了代码片段，没有直接执行文件修改。我现在会立即为你修改代码，以便定位问题。

**计划修改内容：**

1.  **修改 `Phase.c`**:
    *   在 `Get_PhaseDifference` 函数中，添加超时判断时的调试打印 `printf("ADC Timeout!\n");`。
    *   保留之前的超时退出逻辑，防止死循环。

2.  **修改 `main.c`**:
    *   恢复相位计算的调用。
    *   在计算前后添加 `printf` 调试信息，确认程序运行状态。
    *   增加延时，方便观察。

修改完成后，请重新编译下载，并观察串口输出。