## 目标
- 将 [ad9959.c](file:///e:/STM32/F_BEST/BEST_USE/Core/MY_DSP/MY_DDS/ad9959.c) 与 [ad9959.h](file:///e:/STM32/F_BEST/BEST_USE/Core/MY_DSP/MY_DDS/ad9959.h) 中现有英文注释翻译为中文，并为每个对外函数补齐清晰的功能/参数/注意事项注释。
- 新增一个“一键配置通道输出”的函数，替代 [main.c:L143-L146](file:///e:/STM32/F_BEST/BEST_USE/Core/Src/main.c#L143-L146) 里频率/幅值/相位的分散调用。

## 注释改造范围
- 仅改注释文本，不改任何现有函数的逻辑与时序（保持你已验证过的驱动行为不变）。
- 修复当前文件中出现的乱码注释块（用等价中文注释重写）。

## 函数注释补齐策略
- 在 ad9959.h 中为每个对外 API 添加 Doxygen 风格中文注释：用途、参数范围、是否需要 `AD9959_IO_Update()` 生效、典型调用方式。
- 在 ad9959.c 中为关键内部函数（如 `WriteData_AD9959`、`ReadData_AD9959`、`Channel_Select`、`Freq2Word/Amp2Word/Phase2Word` 等）补充中文说明，重点标注：
  - 写寄存器是否“只写缓冲/需 Update”
  - 时序关键点（CS/SCLK/SDIO0 方向切换）
  - 频率控制字的时钟假设（500MHz 对应 20 倍频）

## 新增“一键配置”函数设计
- 在 ad9959.h / ad9959.c 新增函数（不替换、不改动你原有函数）：
  - `void AD9959_SetChannel(uint8_t ch, uint32_t freq_hz, uint16_t phase_deg, uint16_t amp);`
- 该函数内部按你现在 main 的顺序调用：
  - `Write_Frequence(ch, freq_hz);`
  - `Write_Amplitude(ch, amp);`
  - `Write_Phase(ch, phase_deg);`
- 是否自动调用 `AD9959_IO_Update()`：
  - 默认不自动 Update（保持你现有“最后统一 Update”的用法不变），并在注释中明确：调用后需 `AD9959_IO_Update()` 才生效。
  - 如果你希望一行就生效，我再额外提供 `AD9959_SetChannel_Update(...)` 版本（可选，不影响原驱动）。

## main.c 调整
- 将 [main.c:L143-L146](file:///e:/STM32/F_BEST/BEST_USE/Core/Src/main.c#L143-L146) 替换为：
  - `AD9959_SetChannel(0, 20000000, 0, 1000);`
  - 再保留 `AD9959_IO_Update();`

## 验证方式
- 代码改完后做一次全工程编译检查（确保无函数声明/定义不匹配）。
- 用你当前的扫频/切频测试 main 验证：串口输出正常、频率可控（不新增额外依赖）。

如果你确认按这个方案执行，我就开始改这 3 个文件并提交改动。